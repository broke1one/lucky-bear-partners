# 🚀 Пошаговая настройка GitHub Actions для REG.RU

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `lucky-bear-partners` (или любое другое имя)
   - **Description:** "Lucky Bear Partners website"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore" и т.д.
4. Нажмите **"Create repository"**

---

## Шаг 2: Подключите локальный репозиторий к GitHub

**Замените `ваш-username` и `lucky-bear-partners` на ваши данные!**

```bash
cd "/Users/khromeevvadim/patner site"

# Добавьте удаленный репозиторий
git remote add origin https://github.com/ваш-username/lucky-bear-partners.git

# Загрузите код на GitHub
git push -u origin main
```

Если репозиторий уже подключен, просто сделайте:
```bash
git push -u origin main
```

---

## Шаг 3: Получите FTP данные от REG.RU

1. Войдите в панель управления REG.RU
2. Перейдите в раздел **"Хостинг"** → **"FTP-доступ"**
3. Найдите или создайте FTP пользователя
4. Скопируйте:
   - **FTP сервер** (например: `ftp.your-site.ru` или IP адрес)
   - **Логин** (FTP пользователь)
   - **Пароль** (FTP пароль)
   - **Папка** (обычно `/public_html` или `/www`)

---

## Шаг 4: Настройте Secrets в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **"Settings"** (вверху справа)
3. В левом меню выберите **"Secrets and variables"** → **"Actions"**
4. Нажмите **"New repository secret"**
5. Добавьте три секрета:

   **Secret 1:**
   - **Name:** `FTP_HOST`
   - **Value:** ваш FTP сервер (например: `ftp.your-site.ru`)
   - Нажмите **"Add secret"**

   **Secret 2:**
   - **Name:** `FTP_USER`
   - **Value:** ваш FTP логин
   - Нажмите **"Add secret"**

   **Secret 3:**
   - **Name:** `FTP_PASS`
   - **Value:** ваш FTP пароль
   - Нажмите **"Add secret"**

---

## Шаг 5: Проверьте путь на сервере

Откройте файл `.github/workflows/deploy.yml` и проверьте строку:

```yaml
server-dir: /public_html/
```

Если на REG.RU используется другая папка (например `/www`), измените на:
```yaml
server-dir: /www/
```

---

## Шаг 6: Тестируйте деплой

1. Сделайте небольшое изменение в любом файле
2. Закоммитьте и запушьте:

```bash
git add .
git commit -m "Test automatic deployment"
git push
```

3. Перейдите на GitHub в раздел **"Actions"** (вверху)
4. Вы увидите процесс деплоя в реальном времени
5. Если всё зелёное ✅ - деплой успешен!

---

## ✅ Готово!

Теперь при каждом `git push` ваш сайт будет автоматически обновляться на REG.RU!

### Быстрая команда для обновления:

```bash
git add .
git commit -m "Описание изменений"
git push
```

И сайт обновится автоматически! 🎉

---

## 🔧 Решение проблем

### Проблема: "FTP connection failed"
- Проверьте правильность FTP данных в Secrets
- Убедитесь, что FTP сервер доступен
- Проверьте, не заблокирован ли FTP файрволом

### Проблема: "Files not uploaded"
- Проверьте путь `server-dir` в `deploy.yml`
- Убедитесь, что у FTP пользователя есть права на запись

### Проблема: "Action not running"
- Проверьте, что файл `.github/workflows/deploy.yml` загружен в репозиторий
- Убедитесь, что вы пушите в ветку `main`

---

## 📝 Полезные команды

```bash
# Посмотреть статус
git status

# Добавить все изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub (и автоматически на REG.RU)
git push

# Посмотреть историю
git log --oneline
```

---

**Удачи с автоматическим деплоем! 🚀**
