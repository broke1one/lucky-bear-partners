# 📦 Инструкция по загрузке сайта на хостинг

## ✅ Файлы, которые НУЖНО загрузить на хостинг:

### 📄 Основные HTML файлы:
- ✅ `index.html` - главная страница
- ✅ `blog.html` - страница блога
- ✅ `admin.html` - старая версия админки (можно удалить, если используете папку admin/)

### 📁 Папка admin/ (ВСЯ папка):
- ✅ `admin/index.html` - админ-панель
- ✅ `admin/login.html` - страница входа
- ✅ `admin/login.js` - скрипт авторизации

### 🎨 CSS файлы:
- ✅ `styles.css` - основные стили сайта
- ✅ `admin.css` - стили админ-панели

### ⚙️ JavaScript файлы:
- ✅ `script.js` - скрипты главной страницы
- ✅ `blog.js` - скрипты блога
- ✅ `admin.js` - скрипты админ-панели
- ✅ `telegram-link.js` - управление ссылками Telegram

### 📋 Конфигурационные файлы:
- ✅ `.htaccess` - настройки Apache (для работы /admin)

---

## ❌ Файлы, которые НЕ нужно загружать:

- ❌ `IMAGE_SPECS.md` - документация (не нужна на сервере)
- ❌ `DEPLOYMENT.md` - эта инструкция (не нужна на сервере)
- ❌ Любые файлы `.git` или `.gitignore` (если не используете Git на сервере)
- ❌ Файлы с расширением `.md` (Markdown документация)

---

## 📂 Структура файлов на хостинге должна быть такой:

```
/
├── index.html
├── blog.html
├── admin.html (опционально, можно удалить)
├── styles.css
├── admin.css
├── script.js
├── blog.js
├── admin.js
├── telegram-link.js
├── .htaccess
└── admin/
    ├── index.html
    ├── login.html
    └── login.js
```

---

## 🚀 Пошаговая инструкция по загрузке:

### Вариант 1: Через FTP/SFTP клиент (FileZilla, Cyberduck и т.д.)

1. **Подключитесь к хостингу** через FTP/SFTP
2. **Перейдите в корневую папку сайта** (обычно `public_html`, `www`, `htdocs` или `/`)
3. **Загрузите все файлы** из списка выше
4. **Убедитесь, что структура папок сохранена** (особенно папка `admin/`)

### Вариант 2: Через панель управления хостингом (cPanel, Plesk и т.д.)

1. **Войдите в панель управления** хостингом
2. **Откройте файловый менеджер**
3. **Перейдите в корневую папку сайта**
4. **Загрузите файлы** через веб-интерфейс (обычно кнопка "Upload")
5. **Создайте папку `admin`** и загрузите туда файлы из папки admin/

### Вариант 3: Через Git (если настроен)

1. **Инициализируйте Git репозиторий** (если еще не сделано)
2. **Добавьте файлы:** `git add .`
3. **Закоммитьте:** `git commit -m "Initial deployment"`
4. **Загрузите на сервер:** `git push origin main`

---

## ⚙️ Настройки после загрузки:

### 1. Проверьте права доступа к файлам:
```bash
chmod 644 *.html *.css *.js
chmod 755 admin/
chmod 644 admin/*.html admin/*.js
```

### 2. Проверьте работу .htaccess:
- Убедитесь, что на хостинге включен `mod_rewrite` (для Apache)
- Если используете Nginx, настройте редиректы в конфигурации

### 3. Проверьте работу сайта:
- ✅ Откройте главную страницу: `ваш-сайт.com`
- ✅ Проверьте блог: `ваш-сайт.com/blog.html`
- ✅ Проверьте админ-панель: `ваш-сайт.com/admin/`
- ✅ Проверьте авторизацию: `ваш-сайт.com/admin/login.html`

---

## 🔒 Важные замечания:

### Безопасность:
- ⚠️ **Админ-панель защищена только на клиенте** (localStorage)
- ⚠️ Для продакшена рекомендуется добавить **серверную авторизацию**
- ⚠️ Убедитесь, что файлы `.htaccess` загружены (могут быть скрыты)

### Производительность:
- ✅ Все изображения должны быть оптимизированы
- ✅ Используйте CDN для статических файлов (опционально)
- ✅ Включите сжатие GZIP на сервере

### SEO:
- ✅ Убедитесь, что все мета-теги заполнены
- ✅ Проверьте robots.txt (создайте, если нужно)
- ✅ Настройте sitemap.xml (опционально)

---

## 📝 Чек-лист перед загрузкой:

- [ ] Все файлы из списка готовы
- [ ] Проверена работа сайта локально
- [ ] Все ссылки работают
- [ ] Изображения оптимизированы
- [ ] Настроена ссылка на Telegram в админ-панели
- [ ] Проверены права доступа к файлам
- [ ] .htaccess файл готов

---

## 🆘 Если что-то не работает:

### Проблема: Админ-панель не открывается
- ✅ Проверьте, что папка `admin/` загружена
- ✅ Проверьте права доступа к файлам
- ✅ Проверьте работу `.htaccess`

### Проблема: Стили не загружаются
- ✅ Проверьте пути к CSS файлам
- ✅ Проверьте права доступа (chmod 644)
- ✅ Очистите кеш браузера

### Проблема: JavaScript не работает
- ✅ Проверьте консоль браузера на ошибки (F12)
- ✅ Проверьте пути к JS файлам
- ✅ Убедитесь, что все файлы загружены

---

## 📞 Поддержка:

Если возникли проблемы, проверьте:
1. Консоль браузера (F12 → Console)
2. Логи сервера (если есть доступ)
3. Правильность путей к файлам

---

**Удачного деплоя! 🚀**
