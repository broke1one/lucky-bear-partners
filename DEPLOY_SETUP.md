# 🚀 Настройка автоматического деплоя на REG.RU

## Вариант 1: GitHub Actions (Рекомендуется) ⭐

### Шаг 1: Подключите репозиторий к GitHub

```bash
# Создайте репозиторий на GitHub
# Затем подключите его:
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 2: Настройте Secrets в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте следующие секреты:
   - `FTP_HOST` - ваш FTP хост (например: `ftp.your-site.ru`)
   - `FTP_USER` - ваш FTP логин
   - `FTP_PASS` - ваш FTP пароль

### Шаг 3: Готово!

Теперь при каждом `git push` сайт будет автоматически обновляться на REG.RU.

---

## Вариант 2: Локальный скрипт деплоя

### Шаг 1: Настройте скрипт

Откройте файл `deploy.sh` и заполните данные:

```bash
FTP_HOST="ftp.your-site.ru"  # Ваш FTP хост
FTP_USER="your-username"      # Ваш FTP логин
FTP_PASS="your-password"      # Ваш FTP пароль
FTP_DIR="/public_html"         # Папка на сервере
```

### Шаг 2: Сделайте скрипт исполняемым

```bash
chmod +x deploy.sh
```

### Шаг 3: Используйте скрипт

```bash
# После каждого изменения:
git add .
git commit -m "Описание изменений"
./deploy.sh
```

---

## Вариант 3: Git на сервере REG.RU (если есть SSH)

### Шаг 1: Подключитесь к серверу по SSH

```bash
ssh username@your-site.ru
```

### Шаг 2: Настройте Git на сервере

```bash
cd /path/to/your/site
git init
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
```

### Шаг 3: Создайте скрипт обновления на сервере

```bash
# Создайте файл /path/to/your/site/update.sh
cat > update.sh << 'EOF'
#!/bin/bash
cd /path/to/your/site
git pull origin main
EOF

chmod +x update.sh
```

### Шаг 4: Настройте Webhook (опционально)

Создайте endpoint на сервере, который будет вызывать `update.sh` при push в GitHub.

---

## Вариант 4: REG.RU Git (если доступен)

Некоторые тарифы REG.RU поддерживают Git напрямую:

1. Войдите в панель управления REG.RU
2. Найдите раздел "Git" или "Разработка"
3. Подключите ваш GitHub репозиторий
4. Настройте автоматический деплой

---

## 🔧 Получение FTP данных на REG.RU

1. Войдите в панель управления REG.RU
2. Перейдите в раздел "Хостинг" → "FTP-доступ"
3. Скопируйте:
   - FTP сервер
   - Логин
   - Пароль
   - Папка для загрузки (обычно `/public_html`)

---

## 📝 Быстрая команда для деплоя

После настройки GitHub Actions:

```bash
# Просто делайте:
git add .
git commit -m "Описание изменений"
git push

# И сайт обновится автоматически! 🎉
```

---

## ⚠️ Важные замечания

1. **Безопасность:** Никогда не коммитьте пароли в Git! Используйте Secrets в GitHub
2. **Резервные копии:** Делайте бэкапы перед крупными обновлениями
3. **Тестирование:** Тестируйте изменения локально перед деплоем
4. **Права доступа:** Убедитесь, что файлы имеют правильные права (644 для файлов, 755 для папок)

---

## 🆘 Решение проблем

### Проблема: FTP соединение не устанавливается
- Проверьте правильность FTP данных
- Убедитесь, что FTP доступен (не заблокирован файрволом)

### Проблема: Файлы не обновляются
- Проверьте путь к папке на сервере (`FTP_DIR`)
- Убедитесь, что у пользователя есть права на запись

### Проблема: GitHub Actions не работает
- Проверьте, что Secrets настроены правильно
- Посмотрите логи в разделе Actions на GitHub

---

**Удачного деплоя! 🚀**
