# 🔄 Руководство по обновлению сайта на хостинге

## 📋 Что нужно обновлять ВРУЧНУЮ:

### 1. **Файлы сайта (HTML, CSS, JS)**
❌ **НЕ обновляются автоматически** - нужно загружать вручную

**Когда нужно обновлять:**
- Изменили дизайн (CSS файлы)
- Добавили новый функционал (JS файлы)
- Изменили структуру страниц (HTML файлы)
- Добавили новые страницы

**Как обновить:**
1. Замените старые файлы новыми через FTP/файловый менеджер
2. Или используйте Git для автоматического деплоя (см. ниже)

---

### 2. **Данные в localStorage (статьи, настройки)**
⚠️ **Хранятся в браузере пользователя, НЕ на сервере!**

**Важно понимать:**
- Статьи блога сохраняются в `localStorage` браузера каждого пользователя
- Если вы добавили статью в админ-панели на своем компьютере, она будет видна только вам
- Другие пользователи не увидят ваши статьи

**Решение:**
Для продакшена нужно перенести хранение данных на сервер (база данных или файлы на сервере).

---

## ✅ Что обновляется автоматически:

### 1. **Если используете Git + автоматический деплой:**
- Настройте автоматический деплой (GitHub Actions, GitLab CI/CD и т.д.)
- При пуше в репозиторий сайт обновится автоматически

### 2. **Если используете хостинг с синхронизацией:**
- Некоторые хостинги (Netlify, Vercel) могут синхронизировать с Git автоматически

---

## 🚀 Варианты обновления сайта:

### Вариант 1: Ручное обновление через FTP (текущий способ)

**Шаги:**
1. Подключитесь к хостингу через FTP клиент (FileZilla, Cyberduck)
2. Найдите измененные файлы на вашем компьютере
3. Загрузите их на сервер, заменив старые версии
4. Готово!

**Плюсы:**
- Простота
- Полный контроль

**Минусы:**
- Нужно помнить, какие файлы изменились
- Риск забыть загрузить какой-то файл

---

### Вариант 2: Git + автоматический деплой (рекомендуется)

**Настройка:**

1. **Создайте Git репозиторий:**
```bash
cd "/Users/khromeevvadim/patner site"
git init
git add .
git commit -m "Initial commit"
```

2. **Добавьте удаленный репозиторий:**
```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

3. **Настройте автоматический деплой на хостинге:**
   - Многие хостинги поддерживают деплой из Git
   - При каждом `git push` сайт обновится автоматически

**Плюсы:**
- Автоматическое обновление
- История изменений
- Легко откатить изменения

**Минусы:**
- Требует настройки

---

### Вариант 3: Использование хостинга с Git интеграцией

**Популярные варианты:**
- **Netlify** - автоматический деплой из Git
- **Vercel** - автоматический деплой из Git
- **GitHub Pages** - бесплатный хостинг с Git

**Как работает:**
1. Загружаете код в Git репозиторий
2. Подключаете хостинг к репозиторию
3. При каждом обновлении кода сайт обновляется автоматически

---

## ⚠️ ВАЖНО: Проблема с данными (статьи, настройки)

### Текущая ситуация:
- Статьи сохраняются в `localStorage` браузера
- Каждый пользователь видит только свои статьи
- Ваши статьи не видны другим пользователям

### Решение для продакшена:

**Вариант 1: База данных**
- Используйте MySQL, PostgreSQL или MongoDB
- Статьи будут храниться на сервере
- Все пользователи увидят одни и те же статьи

**Вариант 2: JSON файлы на сервере**
- Сохраняйте статьи в JSON файл на сервере
- Читайте статьи через AJAX запросы
- Проще, чем база данных, но менее масштабируемо

**Вариант 3: Headless CMS**
- Используйте Strapi, Contentful, Sanity
- Удобный интерфейс для управления контентом
- API для получения данных

---

## 📝 Чек-лист при обновлении:

### Перед обновлением:
- [ ] Проверьте, какие файлы изменились
- [ ] Протестируйте изменения локально
- [ ] Сделайте резервную копию текущей версии на сервере

### При обновлении:
- [ ] Загрузите все измененные файлы
- [ ] Проверьте права доступа к файлам (chmod)
- [ ] Убедитесь, что структура папок сохранена

### После обновления:
- [ ] Проверьте работу главной страницы
- [ ] Проверьте работу блога
- [ ] Проверьте работу админ-панели
- [ ] Очистите кеш браузера (Ctrl+F5)

---

## 🔧 Рекомендации:

### Для разработки:
1. Используйте Git для версионирования
2. Тестируйте изменения локально перед загрузкой
3. Делайте резервные копии перед крупными обновлениями

### Для продакшена:
1. Настройте автоматический деплой из Git
2. Перенесите хранение данных на сервер (база данных)
3. Настройте мониторинг ошибок
4. Используйте CDN для статических файлов

---

## 💡 Быстрые команды для обновления:

### Если используете Git:
```bash
# Обновить код
git add .
git commit -m "Описание изменений"
git push

# Сайт обновится автоматически (если настроен деплой)
```

### Если используете FTP:
1. Откройте FTP клиент
2. Подключитесь к серверу
3. Загрузите измененные файлы
4. Готово!

---

## ❓ Часто задаваемые вопросы:

**Q: Нужно ли обновлять файлы после каждого изменения?**
A: Да, если вы изменили файлы сайта (HTML, CSS, JS), их нужно загрузить на сервер.

**Q: Обновятся ли статьи автоматически?**
A: Нет, статьи хранятся в localStorage браузера. Для продакшена нужна база данных.

**Q: Как часто нужно обновлять сайт?**
A: Обновляйте только когда вносите изменения. Если ничего не меняли, обновлять не нужно.

**Q: Можно ли настроить автоматическое обновление?**
A: Да, через Git + автоматический деплой или хостинг с Git интеграцией.

---

**Последнее обновление:** 2025
